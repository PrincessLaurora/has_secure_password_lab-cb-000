<%= form_for @user, url: '/login' do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit "Login" %>

  Are you new?: <%= link_to(controller: 'users', action: 'new') { 'sign up' } %>
<% end %>
